# Hadoop Configuration Files

This directory will contain:
- `core-site.xml`: Default filesystem URI (`hdfs://master:9000`)
- `hdfs-site.xml`: Replication factor (set to 2 for 2-worker cluster), NameNode and DataNode directory paths
- `workers`: Worker hostnames (`worker1`, `worker2`)
