# Hadoop Scripts

This directory will contain:
- `hdfs_setup.sh`: Script to format NameNode, start HDFS daemons, and create default directories.
- `upload_dataset.sh`: Helper command to put raw CSV dataset into `/cybersecurity`.
- `check_blocks.sh`: Verifies block distribution across Worker 1 and Worker 2.
