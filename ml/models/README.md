# Saved ML Models

Stores serialized Spark MLlib models and metadata:
- `rf_threat_model/`: Serialized Random Forest model artifact.
- `metadata.json`: Feature importance scores, class labels mapping, and evaluation benchmarks.
