# MongoDB Query Router (mongos) Configuration (Master Node)
# Routes queries and chunk writes across Shard 1 and Shard 2

systemLog:
  destination: file
  path: /var/log/mongodb/mongos.log
  logAppend: true

net:
  port: 27017
  bindIp: 0.0.0.0

sharding:
  configDB: cfgReplSet/configsvr:27019
