# Spark Preprocessing Module

Contains PySpark transformations:
- `clean_logs.py`: Deduplication, null value imputation, IP normalization, and timestamp parsing.
- `feature_engineering.py`: StringIndexer, VectorAssembler, and scaling pipelines for ML consumption.
